# Notes

Served as text/plain, because a browser that is handed text/markdown has no
idea what to do with it and will offer to download the file instead.

Choosing a media type is a decision about what the client should do with the
bytes, not a statement about what the bytes are.
